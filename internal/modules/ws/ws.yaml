format: " {ws} "
cursor: pointer
current_only: false
states:
  urgent: { fg: "@black", bg: "@urgent" }
  active: { fg: "@black", bg: "@active" }
  special: { fg: "@active", bg: "@special" }
on:
  left: goto
